server:
  port: 9001
spring:
  datasource:
    url: jdbc:mysql://localhost:5006/kthirty?autoReconnect=true&useUnicode=true&characterEncoding=utf8&nullCatalogMeansCurrent=true
    username: root
    password: ${KTHIRTY_PASSWORD}
  data:
    redis:
      host: localhost

  liquibase:
    show-summary: off


