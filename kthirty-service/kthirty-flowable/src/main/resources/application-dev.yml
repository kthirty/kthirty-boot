server:
  port: 9002
  servlet:
    context-path: /act

flowable:
  # 自动建表
  database-schema-update: true
  check-process-definitions: true
  async-executor-activate: false
spring:
  datasource:
    url: jdbc:mysql://localhost:5006/test2025?autoReconnect=true&useUnicode=true&characterEncoding=utf8&nullCatalogMeansCurrent=true
    username: root
    password: ${KTHIRTY_PASSWORD}
logging:
  level:
    org.flowable.engine.impl.persistence.entity.*: debug
    org.flowable.task.service.impl.persistence.entity.*: debug
