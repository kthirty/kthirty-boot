package #(backendPackage).mapper;

import top.kthirty.core.db.base.mp.BaseMapper;
import #(backendPackage).entity.#(entityName);

public interface #(entityName)Mapper extends BaseMapper<#(entityName)> {
}
