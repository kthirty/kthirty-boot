server:
  port: 9001
  servlet:
    context-path: /sys
spring:
  datasource:
    url: jdbc:mysql://nas.kthirty.top:5006/kthirty
    username: root
    password: ${password}
  liquibase:
    clear-checksums: true
  data:
    redis:
      host: redis.kthirty.top
      port: 5008
      password: ${password}
